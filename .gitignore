.pio
lib/
# splash_image.h is committed so fresh clones build without running the script.
# Regenerate with: python convert_splash.py  (or --force to overwrite)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Claude Code editor files — keep memory, ignore everything else
.claude/settings.json
.claude/settings.local.json
.claude/worktrees/
.claude/todos/
